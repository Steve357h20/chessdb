{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "https://${BACKEND_URL}/api/$1" },
    { "src": "/admin/(.*)", "dest": "https://${BACKEND_URL}/admin/$1" },
    { "src": "/apidocs/(.*)", "dest": "https://${BACKEND_URL}/apidocs/$1" },
    { "src": "/flasgger/(.*)", "dest": "https://${BACKEND_URL}/flasgger/$1" },
    { "src": "/apispec(.*)", "dest": "https://${BACKEND_URL}/apispec$1" }
  ]
}
